# Public API base URL, not a secret. Copy to .env.local for local submissions.
# Leave unset/empty to keep forms disabled. Restart dev or rebuild after changes.
PUBLIC_STRAPI_URL=http://localhost:1337
